# Force Hyprland to use Intel iGPU only (stable udev symlink, won't change on reboot)
# Requires udev rule: sudo cp /path/to/81-gpu-symlinks.rules /etc/udev/rules.d/
export AQ_DRM_DEVICES="/dev/dri/intel-igpu"
