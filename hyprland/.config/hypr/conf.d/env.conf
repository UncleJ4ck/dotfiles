# Hyprland-specific Environment Variables
# Note: Session-wide env vars are in ~/.config/uwsm/env
# Only add Hyprland-compositor-specific vars here (rare)

# UWSM users: keep HYPR* and AQ_* variables in ~/.config/uwsm/env-hyprland

# Hyprland debug (leave commented unless needed)
# env = HYPRLAND_LOG_WLR,1
# env = AQ_TRACE,1
