# UI / theming env vars are owned by uwsm.
#
# Edit them in:   ~/.config/uwsm/env             (session-wide)
#                 ~/.config/uwsm/env-hyprland    (Hyprland-specific)
#
# This file is kept empty intentionally — any value set here would be
# overridden by uwsm at session start, causing silent drift / confusion.
# Inspect live values with:  systemctl --user show-environment
